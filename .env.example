# AgriGuardian AI Configuration

## Database Configuration
# Options: sqlite:///<path> | postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=sqlite:///agriguardian.db

# For async database operations (PostgreSQL recommended for production)
ASYNC_DATABASE_URL=sqlite+aiosqlite:///agriguardian.db

# Database logging
DB_ECHO=false

## LLM Integration (Optional)
# Leave blank to use rule-based fallback responses
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

## Streaming Configuration

# Required stream source mode: kafka | http | file
STREAM_SOURCE=kafka

# Kafka Configuration (if using Kafka source)
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=sensor-data
KAFKA_GROUP_ID=agriguardian-consumer

# HTTP Polling Configuration (if using HTTP source)
HTTP_ENDPOINT=http://localhost:8080/api/sensors
HTTP_POLL_INTERVAL=5

# File Streaming Configuration (if using file source)
FILE_STREAM_PATH=./sensor_data.jsonl
FILE_BATCH_SIZE=10

## ETL Pipeline Configuration
ETL_WINDOW_SIZE=15
ETL_BATCH_SIZE=100
ETL_PERSISTENCE_ENABLED=true

## RAG System Configuration
RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2
RAG_CHUNK_SIZE=300
RAG_CHUNK_OVERLAP=50
RAG_TOP_K=3
RAG_SIMILARITY_THRESHOLD=0.1

## API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

## Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

## Frontend Configuration
FRONTEND_PORT=8000
WEBSOCKET_URL=ws://localhost:8000/ws
